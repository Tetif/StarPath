# Backend
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/1
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
JPL_HORIZONS_ENABLED=false
CZML_STORAGE_DIR=backend/data/czml

# Frontend (Vite reads env from project root via envDir: "..")
# Leave empty in local dev — Vite proxies /api and /health to the API.
# For Docker builds, set to http://localhost:8000 (or your public API URL).
VITE_API_BASE_URL=
